# Audio Notes

Roman Financial uses Fish Audio for the primary voiceover path.

Connection details:

- Secret key: `.env.local` or `FISH_API_KEY` environment variable.
- Reference voice and model: `pipeline_config.json`.

Do not place secrets in this folder.

